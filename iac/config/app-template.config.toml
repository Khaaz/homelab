# General
PROXMOX_API_URL="https://${IP_SERVER}:8006"
PROXMOX_NODE="home"
PROXMOX_VM_TEMPLATE_NAME="vm-alpine-template"
PROXMOX_VM_TEMPLATE_ID="9000"

# Ansible - setup proxmox
PROXMOX_ADMIN_PASSWORD="${PROXMOX_ADMIN_PASSWORD}"
PACKER_PASSWORD="${PROXMOX_PACKER_PASSWORD}"
TERRAFORM_PASSWORD="${PROXMOX_TERRAFORM_PASSWORD}"

# Packer
CONTROL_NODE_IP="${PROXMOX_TERRAFORM_PASSWORD}"
SSH_ROOT_PASSWORD="${PROXMOX_TEMPLATE_ROOT_PASSWORD}"

# Terraform
