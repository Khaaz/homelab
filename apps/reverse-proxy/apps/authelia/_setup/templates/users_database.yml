users:
  ${USER_NAME}:
    displayname: ${USER_NAME}
    password: ${USER_PASSWORD}
    email: ${USER_EMAIL}
    groups: [admin]
  family:
    displayname: famille
    password: ${AUTHELIA_USER_FAMILY_PASSWORD}
    email: family@no-mail.com
    groups: [family]
